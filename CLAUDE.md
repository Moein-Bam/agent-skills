# Bambuser — agent context

The agent context for this project is maintained once in **AGENTS.md** (the cross‑tool standard), to avoid duplication. Claude Code imports it automatically via the line below; if that import doesn't resolve in your tool, just read [AGENTS.md](AGENTS.md) directly.

@AGENTS.md
